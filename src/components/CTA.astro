---
import type { Locale } from "../config/site.config";
import CTA from "./ui/marketing/CTA/CTA.astro";

export interface Props {
  eyebrow?: string;
  title: string;
  content?: string;
  ctaText?: string;
  ctaHref?: string;
  secondaryText?: string;
  secondaryHref?: string;
  locale: Locale;
}

const {
  eyebrow,
  title,
  content,
  ctaText,
  ctaHref,
  secondaryText,
  secondaryHref,
  locale,
} = Astro.props;
---

<CTA
  eyebrow={eyebrow}
  title={title}
  content={content}
  primaryText={ctaText}
  primaryHref={ctaHref ? ctaHref : undefined}
  secondaryText={secondaryText}
  secondaryHref={secondaryHref ? secondaryHref : undefined}
  centered={true}
/>
