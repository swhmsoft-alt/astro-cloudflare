---
import EntityPillar from "../_EntityPillar.astro";
import type { Locale } from "../../../lib/site-config";
const locale = (Astro.currentLocale || "en") as Locale;
---
<EntityPillar
  collectionName="processes"
  pageTitle="Titanium Manufacturing Processes"
  pageDescription="Comprehensive technical guides for all titanium manufacturing processes. Compare capabilities, tolerances, surface finishes, and applications for 5-axis machining, CNC turning, wire EDM, DMLS, and more."
  locale={locale}
  path="/knowledge/processes/"
/>
