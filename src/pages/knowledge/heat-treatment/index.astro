---
import EntityPillar from "../_EntityPillar.astro";
import type { Locale } from "../../../lib/site-config";

const locale = (Astro.currentLocale || "en") as Locale;
const path = "/knowledge/heat-treatment/";
---
<EntityPillar
  collectionName="heat-treatment"
  pageTitle="Heat Treatment"
  pageDescription="Annealing, solution treatment, aging, and microstructure control for titanium alloys. Process parameters, furnace requirements, and metallurgical outcomes for alpha, beta, and alpha-beta alloys."
  locale={locale}
  path={path}
/>