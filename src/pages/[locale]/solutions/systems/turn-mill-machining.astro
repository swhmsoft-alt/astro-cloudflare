---
import TurnMillPage from "@pages/solutions/systems/turn-mill-machining.astro";
export function getStaticPaths() {
  return [
    { params: { locale: "en" } },
    { params: { locale: "id" } },
  ];
}
const { locale } = Astro.params as { locale: string };
---
<TurnMillPage />
